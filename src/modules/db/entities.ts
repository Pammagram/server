import { ChatEntity, MessageEntity } from '@modules/chat/entities';
import { SessionEntity } from '@modules/session';
import { UserEntity } from '@modules/user/entities';

export const entities = [ChatEntity, MessageEntity, SessionEntity, UserEntity];
